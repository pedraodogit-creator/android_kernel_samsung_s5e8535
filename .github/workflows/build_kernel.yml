name: Build Kernel for A14 5G (Exynos 1330)

on:
  workflow_dispatch:
    inputs:
      allow_placeholder_ramdisk:
        description: "Build sÃ³ do kernel, sem ramdisk Halium real (o boot.img gerado NÃƒO vai funcionar num aparelho)"
        type: boolean
        default: false

env:
  KERNEL_REPO: "https://github.com/devhunter1/android_kernel_samsung_s5e8535.git"
  KERNEL_BRANCH: "lineage-23.2"
  DEVICE_CODENAME: "a14x"
  CLANG_VERSION: "clang-r522817"
  # ATENÃ‡ÃƒO: este Ã© o repo de exemplo do workflow original. O Halium nÃ£o distribui
  # um ramdisk genÃ©rico pronto neste endereÃ§o â€” troque pelo ramdisk real do seu
  # dispositivo (gerado pelo build completo Halium/Android, ou extraÃ­do do
  # boot.img de fÃ¡brica). Veja o aviso na etapa "Download Halium ramdisk".
  HALIUM_RAMDISK_REPO: "https://gitlab.com/halium/ramdisk.git"

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 90
    permissions:
      contents: read
    defaults:
      run:
        shell: bash

    steps:
      # ============================================================
      # 1. INSTALAÃ‡ÃƒO DE DEPENDÃŠNCIAS
      # ============================================================
      - name: Install build dependencies
        run: |
          set -euo pipefail
          sudo apt update
          sudo apt install --no-install-recommends -y \
            bc bison build-essential ccache curl device-tree-compiler \
            flex git gnupg gperf libxml2-utils \
            lzop pngcrush schedtool squashfs-tools xsltproc zip zlib1g-dev \
            libssl-dev python3 python3-pip python-is-python3 libelf-dev cpio rsync \
            gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu \
            gcc-arm-linux-gnueabi binutils-arm-linux-gnueabi

      # ============================================================
      # 2. SETUP DO CLANG (via Google)
      # ============================================================
      - name: Setup Clang
        run: |
          set -euo pipefail
          mkdir -p "${{ github.workspace }}/toolchain"
          cd "${{ github.workspace }}/toolchain"
          echo "ðŸ“¥ Baixando Clang (${{ env.CLANG_VERSION }})..."
          wget -q --show-progress "https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86/+archive/refs/heads/main/${{ env.CLANG_VERSION }}.tar.gz" -O clang.tar.gz
          if [ ! -s clang.tar.gz ]; then
            echo "âŒ Falha no download do Clang!"
            exit 1
          fi
          echo "ðŸ“¦ Extraindo Clang..."
          tar -xf clang.tar.gz
          if [ ! -d "bin" ]; then
            echo "âŒ DiretÃ³rio 'bin' nÃ£o encontrado apÃ³s extraÃ§Ã£o!"
            exit 1
          fi
          chmod -R +x bin/
          echo "${{ github.workspace }}/toolchain/bin" >> "$GITHUB_PATH"
          echo "âœ… Clang configurado com sucesso!"

      # ============================================================
      # 3. CLONE DO KERNEL
      # ============================================================
      - name: Clone kernel source
        run: |
          set -euo pipefail
          echo "ðŸ“¥ Clonando kernel (branch: ${{ env.KERNEL_BRANCH }})..."
          git clone --depth 1 -b "${{ env.KERNEL_BRANCH }}" "${{ env.KERNEL_REPO }}" kernel
          cd kernel
          DEFCONFIG="arch/arm64/configs/${{ env.DEVICE_CODENAME }}_defconfig"
          if [ ! -f "$DEFCONFIG" ]; then
            echo "âŒ Defconfig nÃ£o encontrado em: $DEFCONFIG"
            echo "ðŸ“‚ ConteÃºdo de arch/arm64/configs/:"
            ls -la arch/arm64/configs/ || true
            exit 1
          fi
          echo "âœ… Defconfig encontrado: $DEFCONFIG"

      # ============================================================
      # 4. CONFIGURAÃ‡ÃƒO DO KERNEL (Halium + CorreÃ§Ãµes + olddefconfig)
      # ============================================================
      - name: Configure kernel
        run: |
          set -euo pipefail
          cd kernel
          DEFCONFIG="arch/arm64/configs/${{ env.DEVICE_CODENAME }}_defconfig"

          # Backup do original
          cp "$DEFCONFIG" "$DEFCONFIG.bak"

          # Adiciona opÃ§Ãµes do Halium
          echo -e "\nCONFIG_ANDROID_BINDER_IPC=y" >> "$DEFCONFIG"
          echo "CONFIG_ASHMEM=y" >> "$DEFCONFIG"
          echo "CONFIG_ION=y" >> "$DEFCONFIG"
          echo 'CONFIG_ANDROID_BINDER_DEVICES="binder,hwbinder,vndbinder"' >> "$DEFCONFIG"

          # Desabilita BTF (evita erro do pahole)
          sed -i 's/CONFIG_DEBUG_INFO_BTF=y/# CONFIG_DEBUG_INFO_BTF is not set/g' "$DEFCONFIG"

          # Transforma section mismatches em avisos
          echo "CONFIG_SECTION_MISMATCH_WARN_ONLY=y" >> "$DEFCONFIG"

          echo "âœ… ConfiguraÃ§Ã£o concluÃ­da (backup em $DEFCONFIG.bak)"

      # ============================================================
      # 5. COMPILAÃ‡ÃƒO DO KERNEL (toolchain completo + ccache + olddefconfig)
      # ============================================================
      - name: Cache ccache
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ccache-${{ env.DEVICE_CODENAME }}-${{ github.run_id }}
          restore-keys: |
            ccache-${{ env.DEVICE_CODENAME }}-

      - name: Build kernel
        run: |
          set -euo pipefail
          cd kernel

          export PATH="${{ github.workspace }}/toolchain/bin:$PATH"
          export CCACHE_DIR="$HOME/.ccache"
          export CCACHE_MAXSIZE=5G

          if ! command -v clang &> /dev/null; then
            echo "âŒ Clang nÃ£o encontrado no PATH!"
            exit 1
          fi
          echo "ðŸ”§ Usando Clang: $(clang --version | head -n1)"

          # Array em vez de string: evita depender de word-splitting sem aspas
          MAKE_ARGS=(
            O=out
            ARCH=arm64
            CC="ccache clang"
            LLVM=1
            LLVM_IAS=1
            CLANG_TRIPLE=aarch64-linux-gnu-
            CROSS_COMPILE=aarch64-linux-gnu-
            CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          )

          echo "âš™ï¸ Gerando .config..."
          make "${MAKE_ARGS[@]}" "${{ env.DEVICE_CODENAME }}_defconfig"

          echo "ðŸ”„ Resolvendo opÃ§Ãµes de configuraÃ§Ã£o pendentes (make olddefconfig)..."
          make "${MAKE_ARGS[@]}" olddefconfig

          echo "ðŸ› ï¸ Compilando kernel (usando todos os nÃºcleos)..."
          make -j"$(nproc)" "${MAKE_ARGS[@]}" KCFLAGS="-Wno-error"
          ccache -s || true

          # Prioriza a Image "pura" (o DTB jÃ¡ Ã© anexado Ã  parte no mkbootimg);
          # sÃ³ cai pro find genÃ©rico se nenhuma das opÃ§Ãµes conhecidas existir.
          IMG=""
          for candidate in Image Image.gz Image.gz-dtb Image-dtb; do
            if [ -f "out/arch/arm64/boot/$candidate" ]; then
              IMG="out/arch/arm64/boot/$candidate"
              break
            fi
          done
          if [ -z "$IMG" ]; then
            IMG=$(find out/arch/arm64/boot -maxdepth 1 -name "Image*" -type f | head -n 1)
          fi

          if [ -z "$IMG" ]; then
            echo "âŒ Nenhuma imagem gerada em out/arch/arm64/boot/"
            echo "ðŸ“‚ ConteÃºdo de out/arch/arm64/boot/:"
            ls -la out/arch/arm64/boot/ || true
            exit 1
          fi

          echo "âœ… Imagem encontrada em: $IMG"
          echo "IMAGE_PATH=${IMG}" >> "$GITHUB_ENV"

      # ============================================================
      # 6. UPLOAD DO KERNEL IMAGE (opcional)
      # ============================================================
      - name: Upload kernel Image as artifact
        uses: actions/upload-artifact@v4
        with:
          name: kernel-image
          path: kernel/${{ env.IMAGE_PATH }}
          retention-days: 7

      # ============================================================
      # 7. CLONE DO MKBOOTIMG (repositÃ³rio pÃºblico â€” sem necessidade de token)
      # ============================================================
      - name: Clone mkbootimg
        run: |
          set -euo pipefail
          git clone https://github.com/osm0sis/mkbootimg.git

      - name: Build mkbootimg (remove -Werror)
        run: |
          set -euo pipefail
          cd mkbootimg
          sed -i 's/-Werror//g' Makefile
          sed -i 's/-Werror//g' libmincrypt/Makefile || true
          make
          cd ..
          if [ -f mkbootimg/mkbootimg ]; then
            echo "âœ… mkbootimg compilado com sucesso"
          else
            echo "âŒ Falha ao compilar mkbootimg"
            exit 1
          fi

      # ============================================================
      # 8. BAIXAR RAMDISK DO HALIUM (via Git)
      # ============================================================
      - name: Download Halium ramdisk
        run: |
          set -euo pipefail
          # O "git clone" fica DENTRO da condiÃ§Ã£o do if: assim, se ele falhar,
          # o set -e nÃ£o mata o script antes de chegarmos no fallback abaixo
          # (era esse o bug do workflow original).
          if git clone --depth 1 "${{ env.HALIUM_RAMDISK_REPO }}" halium-ramdisk && [ -f halium-ramdisk/ramdisk.img ]; then
            cp halium-ramdisk/ramdisk.img .
            echo "âœ… Ramdisk baixado com sucesso"
          elif [ "${{ inputs.allow_placeholder_ramdisk }}" = "true" ]; then
            echo "âš ï¸ Ramdisk real nÃ£o encontrado. Criando um vazio sÃ³ para testar a compilaÃ§Ã£o (build NÃƒO instalÃ¡vel)."
            touch ramdisk.img
          else
            echo "âŒ NÃ£o foi possÃ­vel obter um ramdisk Halium real de: ${{ env.HALIUM_RAMDISK_REPO }}"
            echo "   Esse endereÃ§o Ã© sÃ³ um placeholder do workflow original â€” troque HALIUM_RAMDISK_REPO"
            echo "   pelo ramdisk real do seu dispositivo (build completo Halium/Android, ou extraÃ­do do"
            echo "   boot.img de fÃ¡brica com o prÃ³prio mkbootimg/unpackbootimg)."
            echo "   Para compilar mesmo assim sÃ³ para testar o kernel, rode este workflow com"
            echo "   'allow_placeholder_ramdisk: true'."
            exit 1
          fi

      # ============================================================
      # 9. GERAR HALIUM-BOOT.IMG (com DTB)
      # ============================================================
      - name: Generate halium-boot.img
        run: |
          set -euo pipefail
          KERNEL_IMAGE="${{ env.IMAGE_PATH }}"

          if [ -z "$KERNEL_IMAGE" ]; then
            echo "âŒ IMAGE_PATH nÃ£o foi definida!"
            exit 1
          fi

          if [ ! -f "kernel/$KERNEL_IMAGE" ]; then
            echo "âŒ Kernel Image nÃ£o encontrado em: kernel/$KERNEL_IMAGE"
            exit 1
          fi

          # Encontrar DTB (ajuste o filtro do grep pro padrÃ£o real dos .dtb do seu kernel)
          DTB=$(find kernel/out/arch/arm64/boot/dts -name "*.dtb" 2>/dev/null | grep -E "exynos|s5e|a14" | head -1 || true)

          # Array em vez de string concatenada: evita quebrar se algum caminho tiver espaÃ§o
          MKBOOTIMG_ARGS=(--kernel "kernel/$KERNEL_IMAGE" --ramdisk ramdisk.img --output halium-boot.img)

          if [ -n "$DTB" ] && [ -f "$DTB" ]; then
            MKBOOTIMG_ARGS+=(--dtb "$DTB")
            echo "âœ… Incluindo DTB: $DTB"
          else
            echo "âš ï¸ Nenhuma DTB encontrada. O boot pode nÃ£o funcionar."
          fi

          echo "ðŸ“¦ Executando: ./mkbootimg/mkbootimg ${MKBOOTIMG_ARGS[*]}"
          ./mkbootimg/mkbootimg "${MKBOOTIMG_ARGS[@]}"

          if [ -f halium-boot.img ]; then
            echo "âœ… halium-boot.img gerado com sucesso!"
            ls -lh halium-boot.img
          else
            echo "âŒ Falha ao gerar halium-boot.img"
            exit 1
          fi

      # ============================================================
      # 10. UPLOAD DO HALIUM-BOOT.IMG
      # ============================================================
      - name: Upload halium-boot.img as artifact
        uses: actions/upload-artifact@v4
        with:
          name: halium-boot
          path: halium-boot.img
          retention-days: 7
